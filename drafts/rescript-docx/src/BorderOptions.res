type t = {
  style: BorderStyle.t,
  /** Border color, in hex (eg. 'FF00AA'). */
  color?: string,
  /** Size of the border, in 1/8 pt. */
  size?: float,
  /** Spacing offset. Values are specified in pt. */
  space?: float,
}
