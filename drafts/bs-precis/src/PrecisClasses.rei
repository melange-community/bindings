let identifierClass:
  (list(PrecisUtils.codePoint), bool) =>
  list(PrecisUtils.codePoint);
let identifierEnforce:
  list(PrecisUtils.codePoint) =>
  list(PrecisUtils.codePoint);
let freeformClass:
  (list(PrecisUtils.codePoint), bool) =>
  list(PrecisUtils.codePoint);
let freeformEnforce:
  list(PrecisUtils.codePoint) =>
  list(PrecisUtils.codePoint);
